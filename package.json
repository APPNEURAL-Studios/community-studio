{
  "name": "@appneural/community-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Community Studio — served at community.studio.appneural.com",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=20"
  }
}
